{
    "name": "introdemo-backend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
   "build": "cd ../frontend && npm install && npm run build && cp -r dist ../backend && cd ../backend && npm install",
    "start": "node index.js",
     "dev": "node --watch index.js",
     "server": "json-server -p 3001 db.json",
     "build:ui": "rm -rf dist && cd ../frontend && npm install && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui && git add . && git commit -m uibuild && git push"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
